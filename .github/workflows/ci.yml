name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm audit --audit-level=high
      - run: npm run lint --workspace @algorandfoundation/astro-github-loader
      - run: npm test --workspace @algorandfoundation/astro-github-loader
      - run: cd packages/astro-github-loader && npx tsc --noEmit
